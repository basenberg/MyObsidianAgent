"""Type definitions for the core agent module."""

# Model string in pydantic-ai format: "provider:model-name"
type ModelString = str

# Raw message content from OpenAI-format requests
type MessageContent = str
